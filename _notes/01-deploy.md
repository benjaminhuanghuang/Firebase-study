# Deployment

Install Firebase CLI

```sh
npm i -g firebase-tools
```

```sh
npm run build

firebase login

firebase init

    Hosting:
    Select an exiting project on firebase
    public directory: build
    SPA: yes

firebase deploy
```
